import { SpyAdapter } from './spy-adapters/spy-adapter';
export declare class SpyAdapterFactory {
    static get(framework: 'jasmine' | 'jest' | 'noop'): SpyAdapter;
    private static readonly frameworks;
}
